对项目进行消息队列切换改写，将kafka改成pulsar：
1.项目当前具有kafka代码，不要删除
2.需要使用pulsar作为消息队列，发送消息格式为avro
3.需要具有可配置功能，根据配置的type，决定使用哪个消息队列。
4.需要抽象一个sender，sender内部根据type来判断调用，所有的业务直接调用sender来实现mq发送。
5.topic名不要修改，kafka是什么，pulsar就是什么。
验收标准：
单元测试执行通过

这个项目现在发送mq，kafka是json，pulsar是avro，现在我希望以kafka为基准，保持pulsar和kafka发送的一致，你来给方案然后修改。

看一下这个服务涉及哪些topics，哪些是生产者，哪些是消费者。